import groovy.json.JsonSlurper

pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'react-native-android-contract'

def repositoryRoot = file('../../../..')
def nativeContract = new JsonSlurper().parse(
    new File(repositoryRoot, 'sdks/react-native/plugin/native-contract.json')
)

dependencyResolutionManagement {
    versionCatalogs {
        create('superBoardAndroidLibs') {
            from(files(new File(repositoryRoot, 'sdks/android/SuperBoard/gradle/libs.versions.toml')))
        }
    }
}

includeBuild(new File(repositoryRoot, 'sdks/android/SuperBoard')) {
    dependencySubstitution {
        substitute module(nativeContract.android.packageName) using project(':SuperBoard')
    }
}
