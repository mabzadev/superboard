{
  "name": "@superboard/dynamic-links-worker",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.runtime.json",
    "test": "vitest run && INTERNAL_API_TOKEN=dynamic-runtime-secret vitest run --config vitest.runtime.config.ts"
  },
  "dependencies": {
    "@superboard/contracts": "workspace:*",
    "hono": "^4.7.4"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.20.3",
    "@cloudflare/workers-types": "^5.20260808.1",
    "typescript": "^5.8.3",
    "vitest": "^4.1.10",
    "wrangler": "^4.120.0"
  }
}
