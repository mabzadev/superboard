// Compatibility alias for Melody installations that previously selected the
// `smtp` provider. In SuperBoard the Email Worker is the sole SMTP authority.
export { SuperboardMailer as SmtpMailer } from './superboard'
