import { Columns } from "@/components/columns";

export default function FloatingPage() {
  return (
    <>
      {/* The FloatingBanner is automatically inserted into the page by the Flows SDK */}
      <Columns />
    </>
  );
}
