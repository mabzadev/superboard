import { Columns } from "@/components/columns";

export default function TopBannerPage() {
  return (
    <>
      {/* To see how the TopBanner component is inserted into the page, check out the layout.tsx file */}
      <Columns />
    </>
  );
}
