/**
 * Kept temporarily as an import-compatible boundary for downstream extensions.
 * Section links live in the top-level Dashboard plus seven-module navigation.
 */
export function NavProjects() {
  return null;
}
