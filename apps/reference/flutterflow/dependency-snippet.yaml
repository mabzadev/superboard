# Use the reviewed SuperBoard workspace package while developing the reference app.
# The release gate replaces this path with the immutable coordinated SDK tag.
superboard_flutterflow:
  path: ../../sdks/flutterflow
