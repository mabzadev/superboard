# Embedded workflow contract

Files in this nested `.github` directory are retained as the reference
application's acceptance specification and migration history. GitHub does not
execute workflows outside the repository-root `.github/workflows` directory.
The active monorepo CI job is `Reference application` in the root workflow.
