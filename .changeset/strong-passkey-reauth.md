---
"emdash": minor
---

Adds a short-lived strong-reauthentication marker to the EmDash session after
a successful passkey verification, allowing sensitive host operations to
require a recent operator proof without accepting client-declared timestamps.
