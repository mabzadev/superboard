---
"@emdash-cms/admin": minor
---

Adds `readOnly` field descriptors to the content editor for values supplied automatically by the server. These fields display their resolved values and are excluded from manual-save and autosave payloads. Fields remain editable by default.
