---
"@emdash-cms/admin": minor
---

Adds the optional `configurationWhileDisabled` and `lifecycleLocked` plugin metadata fields. Hosts can keep configuration accessible for inactive plugins and lock the activation controls of required plugins. Existing controls retain their behavior when these fields are omitted.
