---
"emdash": patch
"@emdash-cms/admin": patch
---

Fixes the Plugin Manager Enable switch failing for host-managed plugins. Hosts can provide a lifecycle activation route so one click completes the host workflow while preserving plugins that are already active.
