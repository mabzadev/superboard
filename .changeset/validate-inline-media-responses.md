---
"emdash": patch
---

Fixes the inline editor's media picker accepting failed or malformed upload responses. Media metadata is validated before selection, and nullable optional fields remain supported.
