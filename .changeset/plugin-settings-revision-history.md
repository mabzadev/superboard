---
"emdash": minor
---

Records successful plugin settings updates so host integrations can provide a revision history. History includes changed setting names and whether secrets are configured, without copying secret values. Existing settings and rejected updates remain unchanged.
