---
"emdash": patch
"@emdash-cms/admin": patch
---

Fixes the Plugin Manager Disable switch failing for host-managed plugins. Hosts can provide a lifecycle deactivation route that removes the selected plugin from the Front Release while preserving other active plugins.
