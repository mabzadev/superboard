---
"emdash": patch
---

Fixes email signup links opening a JSON response instead of the account creation screen. Signed-out users can reach the signup screen on an initialized site; API clients continue to receive signup verification metadata.
