---
"@emdash-cms/admin": patch
---

Fixes plugin switches retaining their previous state while the plugin list refreshes after a successful activation or deactivation. Managed plugins can expose a Configuration page with lifecycle status, stored settings, health, services, and routes, including while disabled. Secret settings show only whether a value is configured.
